/***
 *
 * Temporary storage spot for a chunk of data that is being trasferred from one place to another.
 * The buffer is filled with data, then passed along.
 * Transfer small chunks of data at a time.
 */
